# Blood Finder Server

Quick Node/Express + SQLite server for development.

Install and run:

```bash
cd server
npm install
npm run dev   # requires nodemon, or npm start
```

API endpoints are mounted under `/api` (donors, requests, messages, banks).
